<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd   http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!--
     The JsonHomeController is using the application/json-home namespace so we have to
     add a message converter for this.
    -->
    <mvc:annotation-driven>
        <mvc:message-converters register-defaults="false">
            <bean class="org.springframework.http.converter.json.MappingJacksonHttpMessageConverter">
                <property name="supportedMediaTypes">
                    <list>
                        <value>application/json-home</value>
                    </list>
                </property>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>

    <!--
     Specify where to find the resources of the example application.
    -->
    <mvc:resources mapping="/jsonhome/**" location="classpath:/jsonhome/" />

    <!--
     The controller used to serve the json-home documents.
    -->
    <bean class="de.otto.jsonhome.controller.JsonHomeController"
          p:maxAgeSeconds = "3600" />

    <!--
     The controller used to serve the HTML representation of the json-home document.
    -->
    <bean class="de.otto.jsonhome.controller.RelController" />

    <!--
     The JsonHomeGenerator to be used by the controllers. You may want to
     choose a different implementation (possibly your own one).
    -->
    <bean class="de.otto.jsonhome.generator.SpringJsonHomeGenerator" />

</beans>