allprojects {
    apply plugin: 'idea'
    apply plugin: 'eclipse'

    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'java'

    version = '0.2.0-SNAPSHOT'

    jar {
        manifest.attributes provider: 'gradle'
    }

}
