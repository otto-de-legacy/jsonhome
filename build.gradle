allprojects {
    apply plugin: 'idea'
    apply plugin: 'eclipse'

    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'java'

    version = '0.1'

    jar {
        manifest.attributes provider: 'gradle'
    }

}
