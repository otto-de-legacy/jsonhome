allprojects {
    apply plugin: 'idea'

    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'java'

    version = '0.1'

    jar {
        manifest.attributes provider: 'gradle'
    }

}
